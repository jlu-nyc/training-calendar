# Bulk Training Data Entry
When given a training plan, format each week as a JSON object with keys: week, day, workout_type, distance_miles, description. Enter all weeks into the appropriate data file, validating totals match expected weekly mileage.
